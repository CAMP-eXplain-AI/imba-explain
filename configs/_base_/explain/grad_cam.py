target_layer = 'layer4.2'

bbox_cfg = dict(bbox_color=(255, 255, 255), thickness=1, is_opaque=False, alpha=0.5)

bbox_label_cfg = dict(
    draw_bg=True, text_bg_color=(255, 255, 255), text_color=(0, 0, 0), top=True, font_scale=0.4, font_thickness=1)
